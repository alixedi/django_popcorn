============
Installation
============

At the command line::

    $ easy_install django_popcorn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_popcorn
    $ pip install django_popcorn